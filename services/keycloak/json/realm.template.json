{
  "realm": "dht-ledger",
  "enabled": true,
  "clients": [
    {
      "clientId": "react-app",
      "enabled": true,
      "publicClient": true,
      "protocol": "openid-connect",
      "directAccessGrantsEnabled": true,
      "standardFlowEnabled": true,
      "rootUrl": "${REACT_CLIENT_URL}",
      "baseUrl": "${REACT_CLIENT_URL}",
      "redirectUris": ["${REACT_CLIENT_URL}/*"],
      "webOrigins": ["${REACT_CLIENT_URL}"]
    }
  ],
  "users": [
    {
      "username": "alunos.teste",
      "email": "alunos.teste@kademlia.local",
      "enabled": true,
      "emailVerified": true,
      "firstName": "Alunos",
      "lastName": "Segurança de sistemas e dados",
      "credentials": [
        {
          "type": "password",
          "value": "${DEFAULT_USER_PASSWORD}",
          "temporary": false
        }
      ]
    }
  ]
}